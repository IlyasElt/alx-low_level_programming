#include "main.h"

/**
 * main - program that prints its name, followed by a new line
 * @argc: count of command line arguments
 * @argv: array of strings passed to the command line arguments
 *
 * Return: 0
 */

int main(int __attribute__((unused)) argc, char *argv[])
{
printf("%s\n", argv[0]);

return (0);
}
