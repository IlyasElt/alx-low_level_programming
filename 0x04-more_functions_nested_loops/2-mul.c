#include "main.h"

/**
 * mul - function that multiplies two integers.
 * @a: first integer to mupltiply.
 * @b: second integer to multiply.
 * Return: the result of @a * @b.
 */

int mul(int a, int b)
{
return (a * b);
}
